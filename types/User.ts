export interface User {
    _id: string; 
    id: string;  
    email: string;
    name: string;
    profilePhoto: string;
    phoneNumber: string;
    createdAt: string;
    username?: string;  
    avatarUrl?: string;  
}